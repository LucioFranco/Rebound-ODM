language: node_js
services:
  - elasticsearch
node_js:
  - "0.12"
  - "0.10"
  - "iojs"
  - "iojs-v1.0.4"

script:
  - npm run coverage

after_success:
  - "cat ./coverage/lcov.info | ./node_modules/.bin/coveralls"

env:
  - COVERALLS_PARALLEL=true
